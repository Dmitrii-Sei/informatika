24.
#include <math.h>
#include <iostream>

using namespace std;
int main()
{
	setlocale(0, "");
	double B, A, C, D;
	cout << "введите значения переменных A B и C";
	cin >> A;
	cin >> B;
	cin >> C;
	D = A;
	A = B;
	B = C;
	C = D;
	cout << "\n A = " << A;
	cout << "\n B = " << B;
	cout << "\n C = " << C;
	return(0);
}

26.
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
	setlocale(0, "");
	double x, y;
	cout << "Введите значение x";
	cin >> x;
	y = 4 * pow((x - 3), 6) - 7 * pow((x - 3), 3) + 2;
	cout << "\n Функция = " << y;
	_getch();
	return 0;
}
27.
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
	setlocale(0, "");
	double x, A;
	cout << "Введите значение переменной A";
	cin >> A;
	x = A * A;
	cout << "\n A в степени 2 =  " << x;
	x = x * x;
	cout << "\n A в степени 4 = " << x;
	x = x * x;
	cout << "\n A в степени 8 = " << x;
	_getch();
	return 0;
}
28.
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
	setlocale(0, "");
	double x, A, y;
	cout << "Введите значение переменной A";
	cin >> A;
	x = A * A;
	cout << "\n A в степени 2 =  " << x;
	y = x * A;
	cout << "\n A в степени 3 = " << y;
	x = x * y;
	cout << "\n A в степени 5 = " << x;
	y = x;
	x = y * y;
	cout << "\n A в степени 10 = " << x;
	y = x * y;
	cout << "\n A в степени 15 = " << y;
	_getch();
	return 0;
}
29. 
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
	setlocale(0, "");
	const double pi = 3.14;
	double x, n, a;
	cout << "Введите значение угла альфа";
	cin >> a;
	n = a * pi / 180;
	cout << "\n Значение угла альфа в радианах " << n;	
	_getch();
	return 0;
30.
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
	setlocale(0, "");
	const double pi = 3.14;
	double x, n, a;
	cout << "Введите значение угла альфа в радианах";
	cin >> n;
	a = n * 180 / pi;
	cout << "\n Значение угла альфа в градусах " << a;	
	_getch();
	return 0;
}
31.
#include <math.h>
#include <iostream>

using namespace std;
int main()
{
	setlocale(0, "");
	double Tf, Tc;
	cout << "Введите значение T в градусах Фаренгейта";
	cin >> Tf;
	Tc = (Tf - 32) * 5 / 9;
	cout << "Температура в градусах Целсия = " << Tc;
	return(0);
}
32.

#include <math.h>
#include <iostream>

using namespace std;
int main()
{
	setlocale(0, "");
	double Tf, Tc;
	cout << "Введите значение T в градусах Цельсия";
	cin >> Tc;
	Tf = Tc * 9 / 5 + 32;
	cout << "Температура в градусах Фаренгейта = " << Tf;
	return(0);
}
33.
#include <math.h>
#include <iostream>

using namespace std;
int main()
{
	setlocale(0, "");
	double X, Y, A, Pr, Pr2;

	cout << "	Введите значение X, Y и A";
	cin >> X;
	cin >> Y;
	cin >> A;
	Pr = X / A;
	Pr2 = Y * Pr;
	cout << "\n Стоимость 1 кг = " << Pr;
	cout << "\n Стоимость Y киллограммов" << Pr2;
		return(0);
}

34.
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
	setlocale(0, "");
	double X, A, Y, B, AX, AB, Delit;
	cout << " X кг конфет = ";
	cin >> X;
	cout << "\n Введите цену за X кг конфет";
	cin >> A;
	AX = A / X;
	cout << "\n Y кг ирисок = ";
	cin >> Y;
	cout << "\n Введите цену за Y кг ирисок";
	cin >> B;
	AB = B / Y;
	Delit = AX / AB;
	cout << "\n Цена за килограмм конфет " << AX;
	cout << "\n Цена за килограмм ирисок " << AB;
	cout << "\n Конфеты дороже ирисок в " << Delit << " раз";
	_getch();
	return 0;
}
35.
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
	setlocale(0, "");
	double V, U, T1, T2, S, Vprotiv, Sozera, Sreki;
	cout << "Введите значение скорости лодки и время T1 ";
	cin >> V;
	cin >> T1;
	Sozera = V * T1;
	cout << "\n Введите скорость течения реки и время T2";
	cin >> U;
	cin >> T2;
	Sreki = (V - U) * T2;
	S = Sreki + Sozera;
	cout << "\n Путь S = " << S;
		_getch();
}
36.
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
	setlocale(0, "");
	double V1, V2, S, T, Put, PS;
	cout << "Введите значение V1 и V2 ";
	cin >> V1;
	cin >> V2;
	cout << "\n Введите значение времени ";
	cin >> T;
	Put = T * (V1 + V2);
	cout << "\n Введите начальное расстояние";
	cin >> S;
	PS = Put + S;
	cout << "\n расстояние между автомобилями через время T =  " << PS;
		_getch();
}
37.
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
	setlocale(0, "");
	double V1, V2, S, T, Put, PS;
	cout << "Введите значение V1 и V2 ";
	cin >> V1;
	cin >> V2;
	cout << "\n Введите значение времени ";
	cin >> T;
	Put = T * (V1 + V2);
	cout << "\n Введите начальное расстояние";
	cin >> S;
	PS = S - Put;
	cout << "\n расстояние между автомобилями через время T =  " << PS;
		_getch();
}
38.
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
    setlocale(0, "");
    double A, x, B;
    cout << "Введите значение A и B";
    cin >> A >> B;
    x = -1 * B / A;
    cout << "\n Значение X = " << x;
    _getch();
}
39.?????
#include <iostream>
#include <conio.h>
#include <cmath>


using namespace std;
int main()
{
    setlocale(0, "");
    double A, C, B, x1, D, x2;
    cout << "Введите значение A, B и с";
    cin >> A >> B >> C;
    D = pow(B, 2) - 4 * A * C;
    x1 = (-1 * B + sqrt(D)) / 2 * A;
    x2 = (-1 * B - sqrt(D)) / 2 * A;
    cout << "значение меньшего корня  = " << x2;
    cout << "\n значение большего корня  = " << x1;
    _getch();
}
